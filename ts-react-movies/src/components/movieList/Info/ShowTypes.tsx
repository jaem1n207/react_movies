import React from "react";
import styled from "styled-components";

interface ShowTypesProps {
  showTypes: Array<ShowTypesProps>;
}
const ShowTypes: React.SFC<ShowTypesProps> = ({ showTypes }) => {
  return <div></div>;
};

export default ShowTypes;
